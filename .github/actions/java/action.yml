name: "Set up JDK 17"
description: "Provisions the JDK used to build and publish this library."

runs:
  using: "composite"
  steps:
    - uses: actions/setup-java@v6
      with:
        distribution: corretto
        java-version: '17'
